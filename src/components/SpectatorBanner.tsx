export function SpectatorBanner() {
  return (
    <div className="spectator-banner">
      <span className="spectator-badge">Spectator</span>
      <span>Watch-only — you cannot bid or join as a captain from this link.</span>
    </div>
  );
}
